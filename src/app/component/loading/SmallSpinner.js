import React from "react";
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'


export default (props)=>{
    return(
        <i style={props.style} className="fa fa-spinner fa-spin"/>
    )
}